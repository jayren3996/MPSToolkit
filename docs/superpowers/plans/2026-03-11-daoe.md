# MPSToolkit DAOE/FDAOE Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add DAOE and FDAOE operator-space projector MPO builders plus Pauli-basis diagnostics to MPSToolkit.

**Architecture:** Implement projector construction under `OperatorSpace` and keep application separate, relying on the existing MPO application machinery. Add small diagnostics for spin and fermionic operator weights, then cover everything with small MPO-based regression tests.

**Tech Stack:** Julia, ITensors.jl, ITensorMPS.jl, MPO/MPS operator-space methods

---

## Chunk 1: Define the operator-space API and tests

### Task 1: Add failing tests for the new public API

**Files:**
- Modify: `/Users/ren/Codex/MPSToolkit/test/test_core.jl`
- Create: `/Users/ren/Codex/MPSToolkit/test/test_operator_space.jl`
- Modify: `/Users/ren/Codex/MPSToolkit/test/test_docstrings.jl`
- Modify: `/Users/ren/Codex/MPSToolkit/test/runtests.jl`

- [ ] **Step 1: Add a module-surface test that `pauli_daoe_projector` and `fdaoe_projector` are exported**
- [ ] **Step 2: Add operator-space tests for small spin projector construction**
- [ ] **Step 3: Add operator-space tests for small fermionic projector construction**
- [ ] **Step 4: Add docstring coverage expectations for the new operator-space functions**
- [ ] **Step 5: Run the targeted tests to confirm they fail before implementation**

## Chunk 2: Implement DAOE/FDAOE projector builders

### Task 2: Replace the DAOE placeholder with actual projector builders

**Files:**
- Modify: `/Users/ren/Codex/MPSToolkit/src/operator_space/daoe.jl`
- Create: `/Users/ren/Codex/MPSToolkit/src/operator_space/bases.jl`

- [ ] **Step 1: Implement low-level local operator-space basis helpers needed for DAOE/FDAOE**
- [ ] **Step 2: Implement `pauli_daoe_projector(...)` returning an MPO projector for spin-1/2 operator space**
- [ ] **Step 3: Implement `fdaoe_projector(...)` returning an MPO projector for fermionic operator space**
- [ ] **Step 4: Add concise docstrings for all new public and internal operator-space helpers**

## Chunk 3: Add diagnostics and verify

### Task 3: Implement operator-space diagnostics

**Files:**
- Modify: `/Users/ren/Codex/MPSToolkit/src/operator_space/daoe.jl`
- Modify: `/Users/ren/Codex/MPSToolkit/src/MPSToolkit.jl`

- [ ] **Step 1: Add operator-weight diagnostics for spin DAOE basis analysis**
- [ ] **Step 2: Add operator-weight diagnostics for fermionic FDAOE basis analysis**
- [ ] **Step 3: Export the approved public API through `MPSToolkit.OperatorSpace`**

### Task 4: Verification

**Files:**
- Verify: `/Users/ren/Codex/MPSToolkit/src/operator_space`
- Verify: `/Users/ren/Codex/MPSToolkit/test/test_operator_space.jl`

- [ ] **Step 1: Run the operator-space test file directly**
- [ ] **Step 2: Run the full package test suite**
- [ ] **Step 3: Add or update at least one example or notebook cell showing projector construction and MPO application**
